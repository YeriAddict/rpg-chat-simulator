$(function() {

    // Handling the Disconnect Button which allows to leave the chat
    $('#disconnect-button').click(function(){
        //var popup = window.open(location, '_blank', '');
        //popup.close();
    });

});